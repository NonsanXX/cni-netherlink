# Server Configuration
PORT=4567

# Node Environment (development, production)
NODE_ENV=production

# Health Check Configuration
PING_TIMEOUT=1500
PORT_CHECK_TIMEOUT=1500

# CORS Configuration (use specific domain in production)
# CORS_ORIGIN=https://yourdomain.com
CORS_ORIGIN=*
